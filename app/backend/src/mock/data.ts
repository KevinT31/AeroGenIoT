export const mockReadings = (count = 24) => {
  return [];
};

export const mockLatestReading = () => null;

export const mockRecommendation = (_reading: any) => null;

export const mockAlerts = () => [];
